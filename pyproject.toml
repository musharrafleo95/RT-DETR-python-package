[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rtdetr_pytorch"
version = "0.1"
dependencies = [
]

[tool.setuptools]
packages = ["rtdetr_pytorch"]
